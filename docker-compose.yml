services:

  gateway:
    image: nginx:alpine
    container_name: api-gateway
    command: /bin/sh -c "envsubst '$$USER_MANAGEMENT_PORT $$SETTINGS_SERVICE_PORT $$FRONTEND_PORT' < /etc/nginx/nginx.conf.template > /etc/nginx/nginx.conf && nginx -g 'daemon off;'"
    volumes:
      - ./devops/nginx.conf:/etc/nginx/nginx.conf.template:ro
    environment:
      - USER_MANAGEMENT_PORT=${USER_MANAGEMENT_PORT}
      - SETTINGS_SERVICE_PORT=${SETTINGS_SERVICE_PORT}
      - FRONTEND_PORT=${FRONTEND_PORT}
    ports:
      - "${GATEWAY_PORT}:80"
    depends_on:
      - user-management-service
      - settings-service
      - frontend

  artemis-database:
    build:
      context: .
      dockerfile: devops/Dockerfile
    container_name: artemis-database
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
      - DB_NAMES=${DB_NAMES}
    ports:
      - "${DB_PORT}:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        - VITE_API_BASE_URL=${VITE_API_BASE_URL}
        - VITE_KEYCLOAK_URL=${VITE_KEYCLOAK_URL}
        - VITE_KEYCLOAK_REALM=${VITE_KEYCLOAK_REALM}
        - VITE_KEYCLOAK_CLIENT_ID=${VITE_KEYCLOAK_CLIENT_ID}
    container_name: frontend
    expose:
      - "${FRONTEND_PORT}"

  user-management-service:
    build:
      context: .
      dockerfile: usermanagement/Dockerfile
    container_name: user-management-service
    ports:
      - "${USER_MANAGEMENT_PORT}:${USER_MANAGEMENT_PORT}"
    environment:
      - SERVER_PORT=${USER_MANAGEMENT_PORT}
      - DB_HOST=artemis-database
      - DB_PORT=5432
      - DB_NAME=user_management_db
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - KEYCLOAK_CLIENT_SECRET=${KEYCLOAK_CLIENT_SECRET}
    volumes:
      - ./data/profile-pictures:/data/profile-pictures
    depends_on:
      - artemis-database

  settings-service:
    build:
      context: .
      dockerfile: settings-service/Dockerfile
    container_name: settings-service
    ports:
      - "${SETTINGS_SERVICE_PORT}:${SETTINGS_SERVICE_PORT}"
    environment:
      - SERVER_PORT=${SETTINGS_SERVICE_PORT}
      - DB_HOST=artemis-database
      - DB_PORT=5432
      - DB_NAME=settings_service_db
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - KEYCLOAK_CLIENT_SECRET=${KEYCLOAK_CLIENT_SECRET}
      - KAFKA_PORT=${KAFKA_EXTERNAL_CLIENT_PORT}
    depends_on:
      - artemis-database

  #  zookeeper:
  #    image: confluentinc/cp-zookeeper:7.5.0
  #    container_name: zookeeper
  #    restart: unless-stopped
  #    # Do not expose internal ports on Production environment
  #    ports:
  #      - "${ZOOKEEPER_CLIENT_PORT}:${ZOOKEEPER_CLIENT_PORT}"
  #    environment:
  #      ZOOKEEPER_CLIENT_PORT: ${ZOOKEEPER_CLIENT_PORT}
  #      ZOOKEEPER_TICK_TIME: 2000
  #    volumes:
  #      - zookeeper-data:/var/lib/zookeeper/data
  #      - zookeeper-log:/var/lib/zookeeper/log
  #  kafka:
  #    image: confluentinc/cp-kafka:7.5.0
  #    container_name: kafka
  #    restart: unless-stopped
  #    depends_on:
  #      - zookeeper
  #    # Do not expose internal ports on Production environment
  #    ports:
  #      - "9092:9092"
  #    environment:
  #      # ---- Zookeeper ----
  #      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
  #      # ---- Broker ID ----
  #      KAFKA_BROKER_ID: 1
  #
  #      # ---- LISTENERS ----
  #      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafkaInternal:9092,PLAINTEXT_HOST://localhost:29092
  #
  #      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_INTERNAL:PLAINTEXT
  #
  #      # ---- Topic & log settings ----
  #      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
  #      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
  #      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
  #      KAFKA_NUM_PARTITIONS: 3
  #      KAFKA_LOG_RETENTION_HOURS: 168
  #      KAFKA_LOG_SEGMENT_BYTES: 1073741824
  #
  #    volumes:
  #      - kafka-data:/var/lib/kafka/data
  #
  #  kafka-init-topics:
  #    image: confluentinc/cp-kafka:7.5.0
  #    container_name: kafka-init-topics
  #    depends_on:
  #      - kafka
  #    entrypoint: [ "/bin/bash", "-c" ]
  #    command: |
  #      "
  #      echo 'Waiting for Kafka to be ready...';
  #      sleep 5;
  #
  #      kafka-topics --bootstrap-server kafka:9092 --create --topic setting.semester --partitions 3 --replication-factor 1 --config cleanup.policy=compact --if-not-exists;
  #      echo 'Created kafka topics:  setting.semester';
  #      "
  #    # Kafka UI - Local development only
  #  kafka-ui:
  #    image: provectuslabs/kafka-ui:latest
  #    container_name: kafka-ui
  #    restart: unless-stopped
  #    ports:
  #      - "8080:8080"
  #    environment:
  #      KAFKA_CLUSTERS_0_NAME: local
  #      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:9092
  #    depends_on:
  #      - kafka


  zookeeper:
    image: confluentinc/cp-zookeeper:latest
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - 22181:2181

  kafkaInternal:
    image: confluentinc/cp-kafka:latest
    depends_on:
      - zookeeper
    ports:
      - 29093:29093
    environment:
      KAFKA_BROKER_ID: 2
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafkaInternal:9093,PLAINTEXT_HOST://localhost:29093
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_CREATE_TOPICS: "settings.semester:1:1"

  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    depends_on:
      - kafkaInternal
    ports:
      - 8100:8080
    environment:
      KAFKA_CLUSTERS_0_NAME: local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafkaInternal:9093
      KAFKA_CLUSTERS_0_ZOOKEEPER: zookeeper:2181
volumes:
  db-data:
  kafka-data:
  zookeeper-data:
  zookeeper-log:
